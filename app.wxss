/**app.wxss**/
page {
  --theme-main: #FF8C00;
  
  /* 🌟 提取的阴影样式（核心要求） */
  /* 图标阴影：四周扩散，颜色较淡，有明显的悬浮感 */
  --icon-shadow: 0 20rpx 40rpx rgba(255, 140, 0, 0.3);
  
  /* 按钮阴影：向下偏移更多，颜色略深，强调可点击性 */
  --btn-shadow: 0 16rpx 32rpx rgba(255, 140, 0, 0.4);
}