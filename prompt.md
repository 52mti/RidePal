你是一个拥有10年经验的资深微信小程序前端开发专家。请根据我上传的墨刀（Modao）高保真设计稿截图，将其高精度还原为微信原生小程序的代码。

【全局要求】
1. 请输出完整的原生微信小程序代码，严格分为 `index.wxml`、`index.wxss`、`index.js` 和 `index.json` 四个独立的代码块。
2. 代码必须完整可运行，**绝对不要使用“// ... 此处省略代码”之类的占位符**，把所有节点的样式和结构都写出来。
3. 如果设计稿中有未明确的图标（Icon）或图片，请统一使用有色块的 `<view>` 标签占位，或者使用微信官方基础图标。

【具体文件要求】
🔴 1. WXML 结构要求：
- 使用语义化的标签（如 `<view>`, `<text>`, `<image>`, `<scroll-view>` 等）。
- 仔细观察截图的布局，理清层级关系，不要把独立的模块写死在一起。
- 尽量使用vant组件库。

🔵 2. WXSS 样式要求（极其重要）：
- **尺寸单位：** 统一使用 `rpx` 进行响应式布局（假设设计稿基准宽度为 750rpx，请根据视觉比例合理预估具体数值）。
- **布局方式：** 尽可能使用 Flex 布局（`display: flex`），处理好主轴和交叉轴的对齐方式。
- **视觉还原：** 仔细提取截图中的背景色（Hex）、文字颜色、字号粗细（font-weight）、圆角（border-radius）、阴影（box-shadow）和内外边距。

🟡 3. JS 逻辑要求：
- 请在 `Page({})` 中定义好基础的 `data` 对象。
- 观察截图中的内容（如列表、卡片），将其转化为具体的 JSON Mock 数据存放在 `data` 中，然后让 WXML 通过 `wx:for` 循环渲染，而不是在 WXML 里写死多个相同的 DOM。
- 为截图上看起来像按钮的元素绑定基础的点击事件（如 `bindtap="handleTap"`），并在 js 中留出空函数。

🟢 4. JSON 配置要求：
- 配置好基础的 `navigationBarTitleText`（根据截图顶部的标题），并设置合适的导航栏背景色和文字颜色。

【补充说明】
这个页面主要的交互逻辑是：[这是一个行程信息页，顶部有两个tab可以切换开车和拼车的行程记录，点击卡片会跳转到行程详情页。开车的行程详情是一个乘客列表页，顶部固定一个发车公告，底部的乘客卡片支持上下滚动，卡片有拖动滑块确认上车的交互功能，点击电话按钮或者信息按钮支持跳转到其它页面。拼车的行程详情顶部固定司机的联系按钮，需要展示腾讯地图，底部一直显示弹窗展示汽车信息和行程信息。]
